"""Panel data methods (FE, RE).

Mirrors the `panel/` directory structure of `engine`/`engine_pybind` (see
`.claude/rules/rust-style.md` "ファイル・ディレクトリ構成"). One module per
method (`fe.py`, later `re.py`), matching `engine_pybind/src/panel/fe.rs`.
"""

from __future__ import annotations
