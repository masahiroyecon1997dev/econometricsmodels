# engine/src/panel/ 実装ノート（FE/RE）

このファイルは `engine/src/panel/` 配下のファイルを読み書きするときだけ自動ロードされる。設計の背景・数式の正本は `docs/planning/specs/panel-api-design.md`（FE/RE論点はすべて確定済み）。ここに書くのは「削除するとClaudeが同じ判断を再発見するはめになる」レベルの実装上の決定事項・罠のみ。

## 実装済み（現状）

- `common.rs`: `PanelError`（FE/RE共有エラー型、Issue #172。`entity`/`time`の長さ不一致用`IdentifierDimensionMismatch`をIssue #175で追加）＋ `PanelDimension` enum ＋ `quasi_demean_column`（θパラメータ化した準偏差変換、Issue #173）＋ `hausman_statistic`（古典的ハウスマン検定の統計量、Issue #174）。
- `fe.rs`: `FeInput`（入力データ型、Issue #175）。`y: Vec<f64>` / `x: Vec<Vec<f64>>` / `entity: Vec<String>` / `time: Option<Vec<String>>`等を保持する薄い入れ物で、`OlsInput`/`IvInput`と異なり`faer::Mat`は組み立てない（`quasi_demean_column`が列単位・`&[f64]`で動く設計のため、`fit()`実装（Issue #178）が生の列をそのまま渡せる。詳細は`fe.rs`モジュールdoc）。`from_columns`は次元検証（`y`↔各`x`列・`y`↔`entity`・`y`↔`time`）のみ行う。
  - `within_transform_one_way`/`within_transform_two_way`（within変換、Issue #176）も実装済み。**2-wayは閉形式の二重デミーニング`ỹ_it = y_it - ȳ_i. - ȳ_.t + ȳ..`を直接計算せず、「entityでquasi-demean（θ=1）→ その結果をtimeでquasi-demean（θ=1）」という2段階の`quasi_demean_column`呼び出しで実装している**（バランスパネルでは数学的に閉形式と厳密に一致することを導出済み・rust-reviewerが独立検算のproptestで検証済み。導出は`fe.rs`モジュールdoc参照）。バランスパネル検証（6.4節）は`validate_balanced_panel`（`fe.rs`内private）が担う: 単純な`n_obs == n_entities * n_periods`のカウント一致だけでは「あるペアの重複と別ペアの欠落が相殺する入力」を見逃すため、`(entity, time)`のユニークペア数が`n_obs`と一致すること（重複なし）も併せて検証する。`time`が未指定なら`PanelError::TwoWayRequiresTime`。
  - `validate_no_zero_variance_regressors`（分散ゼロ説明変数の検出、Issue #177、6.7節）も実装済み。`within_transform_one_way`/`within_transform_two_way`どちらの出力にも使える1-way/2-way共通ロジックで、時間不変変数（1-way）・2-wayでtime FEと完全共線な「エンティティ間で変動しない列」を同じチェックで検出する。**閾値は絶対値ではなく相対値**（`.claude/rules/rust-style.md`「線形代数」の特異性判定の方針と同じ）: `column_is_zero_variance`（`fe.rs`内private）が、within変換後の列の標準偏差を「変換前の元の列の最大絶対値」×「観測数`n`」×`f64::EPSILON`と比較する。変換後の値自身をスケール基準にすると自己参照になり閾値が機能しなくなるため、変換前の値を基準にする（詳細な導出は`column_is_zero_variance`関数docコメント参照）。rust-reviewerレビュー済み（`assert_eq!`での列数・列長の内部契約チェック、n=0境界テストの追加を反映）。
  - `validate_no_singleton_groups_one_way`/`validate_no_singleton_groups_two_way`（singleton検出、Issue #179、6.5節）も実装済み。**within変換の前に生の`entity`/`time`列を直接見る設計**（`within_transform_*`を呼ばない）——6.5節が要求する「下流の特異行列エラーとして偶発的に検出される形にはしない」を満たすため。1-wayはentityのみ、2-wayはentity・time双方を対称に検出する。内部の`reject_singleton_group`（`fe.rs`内private）は`HashMap`でカウント→観測順に再走査して最初のsingletonを報告する2パス方式（グループ間の浮動小数点加算順序に依存しないカーディナリティ集計のみのため`HashMap`でよい、`quasi_demean_column`と同じ理由）。`validate_no_singleton_groups_two_way`は**`time`の存在チェックを最初に行う**（`within_transform_two_way`と同じ順序に揃える設計判断、rust-reviewer指摘を反映）——その後entity→timeの順でsingletonを検出する（両方該当時はentity側を報告、`FeInput`のフィールド順に合わせた恣意的な優先順位）。
  - `FeEffects`（`OneWay`/`TwoWay`）・`FeEstimator`（`OlsEstimator`への委譲、Issue #178、4.3節）も実装済み。`FeEstimator::fit`は`OlsInput::from_columns`を`include_intercept=false`固定で呼ぶ（Frisch-Waugh-Lovell定理によりwithin変換後のOLS点推定はFE推定量`β̂`と数学的に一致する）。`OlsEstimator::fit`自体は常に`cov_type=CovType::Classical`固定で呼ぶ（`β̂`・残差の取得のみが目的で、`cov_type`ごとの標準誤差はFE自身が独立に計算し直すため、委譲先のcov_type選択は結果に影響しない、#181で確定）。パイプラインは「singleton検出（#179）→within変換（#176、2-wayはバランスパネル検証を内包）→自由度検証（#180）→分散ゼロ検出（#177）→`OlsEstimator::fit`への委譲→`cov_type`別の共分散行列計算（#181）→自由度調整後の統計量再計算（#180）」の順（`FeEstimator::fit`のdocコメント参照）。`OlsInput::from_columns`/`OlsEstimator::fit`が返す`LeastSquaresError`は`PanelError::WithinRegressionFailed { source }`に`.map_err`で明示的に包む（`common.rs`の方針、`#[from]`は使わない）。1-way/2-wayの切り替えは`FeEffects` enum（`bool`ではなく、`CovType`と同じ「小さな固定選択肢の公開enum」パターン、ユーザーとの相談で決定）を使う。将来`FeOptions`（Issue #186）が導入されたら、その一部として統合される想定の暫定パラメータ。
  - **自由度調整（Issue #180、6.3節）も実装済み**: `df_model = k + neffects`（1-wayは`neffects=n_entities`、2-wayは`neffects=n_entities+n_periods-1`。entity/timeダミー間の定数項ぶんの重複を`+1`補正）、`df_resid = n - df_model`。`n <= df_model`なら`PanelError::InsufficientDegreesOfFreedom`（この検証は`OlsEstimator::fit`自身の`n<=k`チェックより常に厳格——`neffects>=1`のため`df_model>k`が恒常的に成り立つ——なので、`FeEstimator::fit`経由でOLS側の`InsufficientObservations`が発生することは構造的にない）。t値・p値・信頼区間の自由度は`cov_type`によらず常に`df_resid`（3.3節、#181実装時にlinearmodelsで確認）で、`crate::inference`の共有ヘルパー（OLS自身と同じロジック）から計算する。AIC/BICは`log_likelihood`自体は`SSR/n`のみに依存しdf非依存の式のため`OlsEstimator::log_likelihood()`をそのまま再利用し、ペナルティ項の乗数だけ`k`から`df_model`に差し替える。
  - **F統計量（`f_statistic`/`f_p_value`）も実装済み（Issue #186、当初Issue #180のスコープ外だったが、`panel-api-design.md`2.1節がOLS同様含める前提だったことがIssue #186のフィールド設計時に判明し前倒し実装）**: 傾き係数`k`個が同時にゼロという帰無仮説のWald F検定（`linearmodels.PanelOLS.f_statistic`「H0: All parameters ex. constant are zero」と同じ定義。固定効果ダミー自体は検定対象に含めない——`fixest`の`fitstat(m, "f")`はFEダミーも含めたモデル全体のF検定で定義が異なるためクロスチェックに使わない）。`crate::linear::ols::wald_f_test`（元々`OlsEstimator`本体・`wald_test_last_columns`が使う`fn`だったものを`pub(crate)`化）をFEの`cov_type`別`cov_params`・`df_resid`で呼ぶ形で再利用し、サンドイッチ計算を複製しない（`k_constant=0`固定、FEに切片は無いため）。**`estimator().f_statistic()`/`f_p_value()`は引き続き`df_resid_ols = n - k`・`CovType::Classical`ベースのままで誤り**（`FeEstimator`自身の`f_statistic()`/`f_p_value()`を使うこと）。失敗（共分散部分行列のほぼ特異性、`wald_f_test`のbackstop）は新設した`PanelError::FTestFailed { source }`（`WithinRegressionFailed`とは意味が異なるため別バリアント）。**この失敗経路のFE専用再現テストは意図的に見送っている**——`cov_type=Classical`である限り、委譲先の`OlsEstimator::fit`自身が同じ設計行列に対する同種のF検定を内部で無条件に計算しており、そちらが先に`WithinRegressionFailed`として失敗するため、OLSの`fit_returns_computation_failed_for_extreme_scale_difference_in_f_test`と同型の入力ではFE側の`FTestFailed`まで到達しない（実地確認済み。`wald_f_test`自体の正しさはOLS側のテストで担保される）。検証は`linearmodels`（`cov_type="unadjusted"`）の`f_statistic`と直接比較（`f_test_reference_input`、k=2の新規フィクスチャ、`FeCovType::Classical`のみ）。k=1のケース（`fixest_reference_input`）は「1自由度のF検定は両側t検定と代数的に等価」という恒等式（`f_statistic = t_stat²`）で追加検証している——**この恒等式チェックは、rust-reviewerの指摘（初回実装ではClassical以外の`cov_type`でF統計量が一切検証されていなかった）を受けて、HC1/HC2/HC3/Cluster（no-rescale・with-rescale両方）/HACの既存k=1テストすべてに追加した**。これにより「`cov_type`別に計算し直した`cov_params`・`df_resid`が正しく`wald_f_test`に渡っているか」をcov_type横断で回帰ガードできる。
  - **検証の例外（Issue #180）**: Python主リファレンスの`linearmodels`（`PanelOLS`）は`aic`・`bic`を一切提供しない（ソースコード確認済み。`rsquared_within`/`between`/`overall`/`inclusive`・`loglik`のみ）ため、この2つの検証はRクロスチェック（`fixest`）のみで行う（通常の「Python主リファレンス+Rクロスチェック」の2系統検証の例外、ハウスマン検定（5.3節）と同型）。上記の式は`fixest::feols`の`AIC()`/`BIC()`と数値的に一致することを実地検証済み（`fe_estimator_fit_one_way_matches_fixest_reference`/`_two_way_matches_fixest_reference`で固定）。
  - **パネル固有R²（Issue #183、2.3節）も実装済み**: `r_squared_within`/`r_squared_between`/`r_squared_overall`の3フィールド（`bareのr_squared_adj`は廃止、2.3節が明示的に要求）。素朴に「実際に使ったFE構造でdemeanしたR²」を3種とも定義すると考えがちだが、`linearmodels==7.0`のソース確認・実地数値検証で以下が判明している（ユーザーとの相談で決定、2026-09-12。詳細な式・導出は`fe.rs`モジュールdoc「パネル固有R²」）:
    1. **`r_squared_within`は「実際に使ったFE構造でdemeanした残差」を採用**（1-wayはentityのみ、2-wayはentity+time）——`estimator().r_squared()`をそのまま使う。**`linearmodels`自身の`rsquared_within`は常にentityのみのdemeanで固定**（2-wayモデルでも時間効果を含めない）という別定義のため、2-wayでは意図的に数値が食い違う。2-wayの検証はユーザー指示により`linearmodels`ではなく`fixest`の`fitstat(model, "wr2")`（"Within R2"）を使う（実地検証で1-way・2-way双方の数値一致を確認済み。AIC/BICと同型のRクロスチェック例外）。
    2. **`r_squared_between`/`r_squared_overall`は`linearmodels`の`_rsquared`と完全一致**させる。FEのxには定数列を含められない（含めれば`validate_no_zero_variance_regressors`が弾く）ため`has_constant=False`分岐（非中心化TSS）が常に適用される。`r_squared_overall`は固定効果の切片項を一切含めない元の`y`・`x`に`β̂`だけを当てはめた残差ベース（`estimator().residuals()`＝FWL定理による固定効果込みの残差とは別物）。`r_squared_between`はエンティティ平均`ȳ_i.`・`x̄_i.`に`β̂`を当てはめた残差ベース。
    3. **`r_squared_between`にエンティティ観測数`T_i`による重み付けをしてはいけない罠**: `linearmodels`の`_prepare_between`自体は不均衡パネル用の重み`w_i=T_i/mean(T)`を計算するが、`_rsquared`側でサンプルウェイト（`weights`引数）が全て`1.0`（未指定）なら`w`を無条件に`1.0`へ上書きする。**FEは`weights`引数をサポートしない**（CLAUDE.md 1.3節）ためこの重み付けは常に無効。一度`T_i`ベースの重み付き版を実装し、バランスパネルのテストでは`w_i=1`に退化するため気づかず、不均衡パネルで初めて`linearmodels`との数値不一致（0.9448 vs 0.9438）が発覚して修正した（`fe_estimator_fit_one_way_r_squared_between_matches_linearmodels_on_unbalanced_panel`で固定。バランスパネルのみのテストだと重み付けロジックの分岐が一度も実質的に検証されない典型例、Issue #182のDKバンド幅ループと同じ教訓）。
    - 新規ヘルパー（`fe.rs`内private）: `fe_r_squared_between`・`fe_r_squared_overall`（`group_indices_by_key`をentity集計に再利用）。
  - **`cov_type`対応（Issue #181、3.1節・3.2節）も実装済み**: `FeCovType` enum（`Classical`/`Hc1`/`Hc2`/`Hc3`/`Cluster{groups}`/`Hac{bandwidth}`）を`OlsEstimator`の`CovType`とは別に新設（HC0を含まない、無効な組み合わせを型で表現不可能にする設計）。**`OlsEstimator`の既存cov_type計算式（`classical_cov_params`/`hc_cov_params`/`cluster_cov_params`）はそのまま流用できない**——linearmodels/fixestのソース確認・実地数値検証で判明した3点の相違（詳細は`fe.rs`モジュールdoc「`cov_type`対応」）:
    1. HC0はスコープ外（linearmodels・fixestともにパネル/FE向けのHC0オプションが存在しない）。
    2. HC1〜HC3は独自計算: HC1は小標本補正係数が`n/(n-k)`ではなく`n/df_resid`。HC2/HC3のレバレッジは、within変換後の設計行列ではなくLSDV相当のフルレバレッジ`h_ii_full = 1/T_entity(i) + h_ii_within`（1-wayは分割回帰＝Frisch-Waugh-Lovellのレバレッジ分解則、2-wayはさらに`+ 1/N_time(i) - 1/n`）でなければならない（fixestと数値一致確認済み）。
    3. Clusterも独自計算: OLSは`(G/(G-1))×((n-1)/(n-k))`というStata流の小標本補正を常に適用するが、linearmodels（FEの主リファレンス）はこの補正を使わず`n/(n-extra_df-k)`のみを使う。**`extra_df`はcluster変数とFEの関係で決まる**（linearmodelsの`_determine_df_adjustment`と数値一致確認済み）: 1-way FEで「クラスター変数がentityと同じか、entityを包含するより粗い分割（各entityが単一のクラスターに属する、`entity_nested_within_cluster`で判定）」なら`extra_df=0`（`cluster_col`省略時のデフォルト——entity自体——は常にこの条件を満たす）、それ以外（1-way FEでentityと無関係なクラスター変数、または2-way FE）は`extra_df=neffects`。
    - 新規ヘルパー（`fe.rs`内private）: `design_matrix_from_columns`（列→`faer::Mat`組み立て）・`xtx_inverse`（`(X̃'X̃)⁻¹`、`OlsEstimator`のprivateな`cov_params`を再利用できないため独立に計算し直す）・`leverage_within`/`leverage_full`・`fe_classical_cov_params`/`fe_hc_cov_params`/`fe_cluster_cov_params`・`entity_nested_within_cluster`・`group_sizes`。
    - `cov_type`のデフォルト（`"cluster"`、entity単位、3.2節）・`cluster_col`文字列パースは`engine_pybind`層（`FeOptions`、Issue #186以降）の責務。`FeEstimator::fit`自体はデフォルトを持たない。
  - **Driscoll-Kraay型パネルHAC対応（Issue #182、3.1節）も実装済み**: `FeCovType::Hac { bandwidth: Option<i64> }`。着手時に`linearmodels.panel.covariance.DriscollKraay`のソースを実地確認し、以下をユーザー承認済み（2026-09-12、詳細な式・導出は`fe.rs`モジュールdoc「Driscoll-Kraay型パネルHAC対応」）:
    1. **カーネルはv1でBartlett限定**（OLSの`CovType::Hac`と平仄を合わせる）。Parzen/QSへの拡張はIssue #313（未着手）。
    2. **バンド幅**は`linearmodels`のデフォルトルール`floor(4*(t/100)^(2/9))`（`t`=ユニークな時点数、OLSの`hac_lags`が観測数`n`ベースなのと違う点に注意）を`resolve_dk_bandwidth`で踏襲。明示指定は`[0, t)`範囲検証（`PanelError::InvalidHacBandwidth`）。
    3. **時系列順序は`time: Vec<String>`の辞書順とみなす**（`entity`と同じく同一性のみが意味を持つ列で順序情報を持たないため。ISO 8601日付・ゼロ埋め年度等、辞書順=時系列順になる形式で渡すことが呼び出し側の契約——`engine`側にこの契約のバリデーションは無い）。`fe_driscoll_kraay_cov_params`は`BTreeMap<&str, Vec<usize>>`で`time`をキーに集計し、辞書順＝時系列順を一致させている。
    4. **1-way/2-way両対応**。2-way FEは`time`が既に必須だが、1-way FEで`FeCovType::Hac`を指定したのに`time`が`None`なら`PanelError::HacRequiresTime`。
    5. スケールは`(n/df_resid) × (X̃'X̃)⁻¹ Ŝ (X̃'X̃)⁻¹`。linearmodelsは`cov_type="kernel"`で常に`extra_df=neffects`（`Cluster`と異なり`entity_nested_within_cluster`のような条件分岐が無い）かつデフォルト`debiased=True`のため、素直に本モジュールの`df_resid`と一致する。
    - **回帰テストとしての交差検証**: `bandwidth=Some(0)`（ラグ項なし）は`FeCovType::Cluster { groups: Some(time) }`（entityと無関係なクラスター変数、`extra_df=neffects`）と数式的に同一になるため、2つの独立実装（`fe_cluster_cov_params`/`fe_driscoll_kraay_cov_params`）の一致を確認するテストを追加している（OLSの`fit_hac_with_zero_lags_matches_hc0`と同型の手法）。
    - **`FeCovType::Hac.time`による時系列順序の明示的な上書き（Issue #186で追加）**: `Hac { bandwidth: Option<i64>, time: Option<Vec<String>> }`に拡張した（元は`bandwidth`のみ）。`engine_pybind`のFeOptions設計時に「2-way FEの`time`（固定効果構造）とDK HACの時系列順序を別の列にしたい」というユースケースが判明したため（ユーザー承認済み、2026-09-12）。`time`が`Some`なら`input.time()`より優先してDK計算に使う（`FeInput.time()`が`None`の1-way FEでもこれだけでDK HACが成立する）。`FeInput`自体は変更していない——`time`はcov_type固有のオプションであり、パネル構造（2-wayの有無）とは独立に指定できる設計。
  - **固定効果自体（α_i）の復元（Issue #184、6.6節）も実装済み**: `FeEstimator::fixed_effects()`（`fit()`の戻り値本体には含めない別メソッド、IVの`first_stage()`と同じ方針）。1-wayは`FixedEffects::OneWay(BTreeMap<String, f64>)`、2-wayは`FixedEffects::TwoWay { entity, time }`。**1-wayは一意**（`α_i = ȳ_i. - x̄_i.'β̂`、切片が全てentityに吸収される設計のため正規化の任意性が無い）だが、**2-wayには正規化の任意性がある**（着手時に発見、ユーザー承認済み、2026-09-12）: `α_i`に定数`c`を足し`γ_t`から`c`を引いても同じ予測値になるため一意に決まらず、6.6節の式をそのままentity/timeに当てはめると大域平均が二重計上されるバグになる。**採用した規約: `time`の辞書順で最初の値`t_ref`を基準に`γ_{t_ref}=0`とし、`α_i`に大域的な水準を吸収させる**（DKの時系列順序規約と同じ、観測順に依存しない決定的な選び方）。**`fixest::fixef()`との数値一致は`t_ref`の選び方が一致する入力に限られる**——実地検証で`fixest`自身の基準時点選択は`time`列の辞書順ではなく観測順で最初に現れた値に見えることが判明（同じ`{entity,time}`ペア集合でも行の並び順を変えると`fixef()`の基準時点が変わる）。2-wayの正規化はどの`t_ref`を選んでも数学的に等価なため、`fixest`の観測順依存の挙動は再現せず辞書順の規約を優先する（ユーザーとの相談で決定。詳細な導出は`fe.rs`モジュールdoc「固定効果自体（α_i）の復元」参照）。新規ヘルパー（`fe.rs`内private）: `slope_only_residual`（`fe_r_squared_overall`と共有）・`group_residual_means`・`overall_residual_mean`。
- `re.rs`: `ReInput`（入力データ型、Issue #192）。`FeInput`（`fe.rs`、Issue #175）とほぼ同一の構造・`from_columns`次元検証（`y`↔各`x`列・`y`↔`entity`・`y`↔`time`）を持つ薄い入れ物（`y: Vec<f64>` / `x: Vec<Vec<f64>>` / `entity: Vec<String>` / `time: Option<Vec<String>>`等）。**RE自身の準偏差変換（7.2節）はentity方向のみで`time`を使わない**が、`ReInput`は`FeInput`と同じ`time`フィールドを保持する——理由は`RE.fit()`が内部でFE推定を実行してハウスマン検定の比較対象を得る際（2.4節）に「`entity`/`time`/`x`はRE呼び出し時と同一の指定を使う」ため（`ReOptions.time`、1.1節）。このハウスマン検定用の内部FE呼び出しロジック自体は未実装（ハウスマン検定を実装するIssue #198で対応。`swamy_arora_variance_components`が呼ぶ内部FE推定——σ_ε²用、常に1-way・`FeCovType::Classical`固定——とは別の呼び出しである点に注意）。
  - **Swamy-Arora分散成分推定（`swamy_arora_variance_components`、Issue #193、7.1節）も実装済み**: σ_ε²は内部1-way FE推定（`FeEstimator::fit(OneWay, Classical)`）のwithin回帰残差平方和／`FeEstimator::df_resid()`、σ_u²はbetween回帰（エンティティ平均への`OlsEstimator::fit(include_intercept=true)`）のSSR／`OlsInput::nobs()-k()`から、調和平均`t_bar`を使う標準式`max(0, ssr/df_resid - σ_ε²/t_bar)`で求める。**`panel-api-design.md`7.1節の式（分母`n-k-n_entities+1`・`n_entities-k`）は`linearmodels`ソースの`nvar`（切片を含む列数）表記の転記であり、このプロジェクトの`k`規約（傾き係数のみ）ではFE/OLS委譲先が実際に使った`df_resid`をそのまま再利用すれば自動的に一致する（`+1`/`-1`を式に手で足し引きする必要が無い）**——乱数・手動データ複数ケースで`linearmodels.RandomEffects`との数値完全一致を実地検証済み（ユーザー確認済み、2026-09-13、詳細な導出は`swamy_arora_variance_components`関数doc参照）。
  - **落とし穴: 内部FE推定は`FeInput`と同じsingleton検証（Issue #179）を継承するため、`T_i=1`のエンティティがあるデータではσ_ε²計算が`PanelError::SingletonGroup`で失敗する**。一方`linearmodels.RandomEffects`自身はsingletonエンティティを問題なく処理できる（該当行のentity-demean値が単に0になるだけで、REの数学的定義自体はsingletonを許容する）。つまり**このプロジェクトのRE実装は「FEをwithin回帰の再利用先にする」設計上の帰結として、`linearmodels`より厳格にsingletonを拒否する**（意図的な既定路線として実装済み、`panel-api-design.md`7.4節「σ_ε²の推定はFEのwithin回帰の残差分散をそのまま利用する」の自然な帰結）。テストフィクスチャ・ベンチマーク作成時（後続issue）に、singletonエンティティを含むデータで`linearmodels`と比較すると数値ではなく失敗/成功が食い違う点に注意——ベンチマークデータは全エンティティ`T_i>=2`を確保すること。
  - between回帰は切片+傾き`k`個で`k+1`パラメータのため、`n_entities > k+1`が必要（`n_entities <= k+1`だと`OlsEstimator::fit`自身の`n<=k`検証で`PanelError::BetweenRegressionFailed`。`linearmodels`自身も`n_entities=k+1`ちょうどで`ZeroDivisionError`になることを実地確認済み——`neffects-nvar=0`）。
  - 新規共有ヘルパー: `group_indices_by_key`・`count_unique`は元々`fe.rs`のprivate関数だったが、REのbetween回帰（エンティティ平均集計）でも必要になったため`common.rs`に`pub(crate)`として移設した（`.claude/rules/rust-style.md`「系統内で共有するロジックは`<系統>/common.rsに置く`」、Issue #193）。`fe.rs`側は移設後もimportを変えるだけで動作は不変（既存574テストで確認済み）。
  - `entity_means`（`re.rs`内private）はエンティティごとの`y`/`x`平均と観測数`T_i`を一度の集計で返す（between回帰・`t_bar`計算の二重集計を避ける）。
  - `swamy_arora_variance_components`は本来`ReEstimator::fit`（Issue #195）専用の内部実装詳細で`pub(crate)`が適切だが、Issue #195着手前の現時点では非テストコードからの呼び出しが無く`pub(crate)`だと`dead_code`警告になるため`pub`にしている（`quasi_demean_column`・`hausman_statistic`と同じ前例、`common.rs`参照）。
  - `PanelError::BetweenRegressionFailed { source: LeastSquaresError }`（Issue #193）を追加した。`WithinRegressionFailed`と同じラップだが対象がFEのwithin回帰ではなくREのbetween回帰のため別バリアントにした（`FTestFailed`と同じ判断）。**追加時は`engine_pybind/src/panel/common.rs`の`panel_error_to_pyerr`（網羅的`match`）も忘れず更新すること**——Issue #193実装時に一度更新漏れで`cargo build --workspace`が`E0004`で失敗した（`engine_pybind/src/panel/CLAUDE.md`「踏んだ罠」参照。`cargo test -p engine`だけでは検出できない）。
  - **θ計算・準偏差変換（`compute_theta`・`quasi_demean_transform`、Issue #194、7.2節）も実装済み**: `θ_i = 1 - sqrt(σ_ε² / (T_i・σ_u² + σ_ε²))`をエンティティごとに計算し（`compute_theta`、`group_indices_by_key`で`T_i`を集計）、`quasi_demean_column`を`y`・各`x`列に適用する（`quasi_demean_transform`。FEの`within_transform_one_way`と同型のパターンで、θ=1固定かエンティティごとに異なるθかだけが違う）。REはentity方向のみ（7.2節）のため不均衡パネルも無条件でサポートし、**`swamy_arora_variance_components`と異なり内部でFE推定を呼ばないため、singletonエンティティ（`T_i=1`）を含むデータでもこの変換自体はエラーにならない**（singleton制約は`swamy_arora_variance_components`側だけの制約であり、`compute_theta`/`quasi_demean_transform`は`σ_ε²`/`σ_u²`をf64で受け取るだけで内部にFE呼び出しを持たないため）。`quasi_demean_transform`は`theta`自体も返す——Issue #195で切片復元用の定数列（すべて1.0）に同じ`theta`を適用する際、再計算を避けるため。
  - **`OlsEstimator`への委譲（`ReEstimator`、Issue #195、7.4節）も実装済み**: パイプラインは「`swamy_arora_variance_components`（Issue #193）→`quasi_demean_transform`（Issue #194）→切片復元用の定数列（すべて1.0）を同じθで`quasi_demean_column`→`OlsEstimator::fit(include_intercept=false)`への委譲」の順（`FeEstimator::fit`と同型のパターン）。**REはFEと異なり切片を持つ**ため、この定数列の準偏差変換が必要——単純に`OlsInput::from_columns`の`include_intercept=true`を使うと変換されない生の`1.0`列になってしまう（`linearmodels.RandomEffects.fit()`のソースで、`exog`に含まれる定数列自体も他の説明変数と同じ`quasi_demean`処理を受けていることを確認済み。手動データで`linearmodels`との数値完全一致を実地検証済み）。`param_names`は`OlsInput`の自動`"const"`命名と同じ並び（`["const", x_names...]`）に手動で揃える。`OlsEstimator::fit`自体は`FeEstimator::fit`と同じ理由で`CovType::Classical`固定。**この時点では係数推定（`β̂`）とdf_resid/df_model（Issue #196、次項）の委譲のみがスコープ**（F統計量・パネル固有R²はFE同様RE固有の補正が必要で別Issue、`FeEstimator`の#178時点と同じ扱い）。
    - `swamy_arora_variance_components`・`compute_theta`・`quasi_demean_transform`は`ReEstimator::fit`という非テストの呼び出し元ができたため、`pub`から`pub(crate)`に格下げした（Issue #194時点の申し送り事項を解消）。
    - `PanelError::QuasiDemeanedRegressionFailed { source: LeastSquaresError }`を新設（`WithinRegressionFailed`・`BetweenRegressionFailed`と同型のラップだが対象がREの最終委譲のため別バリアント）。追加と同時に`engine_pybind/src/panel/common.rs`の`panel_error_to_pyerr`も更新済み（Issue #193の教訓を踏まえ、同一コミットで対応）。
    - **`confidence_level`が不正な場合、`QuasiDemeanedRegressionFailed`ではなく`WithinRegressionFailed`として伝播する**——`swamy_arora_variance_components`内部の最初の委譲（`FeEstimator::fit`）が真っ先に検証するため（`re_estimator_fit_propagates_invalid_confidence_level_error`で固定）。
    - **σ_ε²=0の2つの下位ケースを区別すること（Issue #194→#195でrust-reviewerが指摘、Issue #195で実地確認済み）**:
      - **σ_ε²=0かつσ_u²=0の同時ゼロ**: `θ_i`の計算式自体が`0.0/0.0`でNaNになり、`quasi_demean_column`経由でy/xの全観測値にNaNが伝播する。実務上どちらも厳密に0になるのは極めて稀（Phase4のベンチマーク・テストでこのケースを意図的に作る予定は無い）ため、本Issueでは対応を見送った——NaN伝播をそのまま許容するか`ComputationError`で明示的に弾くかは、実際に問題になった時点で`ReEstimator::fit`内での対応を検討する。
      - **σ_ε²=0単独（σ_u²>0）**: NaNにはならず`θ_i=1`（全エンティティ）に収束し、切片復元用の定数列`1-θ_i・1`が恒等的に全ゼロ列になるため、`OlsEstimator::fit`が`LeastSquaresError::SingularMatrix`で確実に失敗する（NaNではなく明示的な計算エラーとして正しく検出される「意図通りの失敗」）。ノイズ無しの線形DGP（内部FE推定のwithin残差が厳密に0になる）で現実的に到達しうる経路であることを`re_estimator_fit_returns_quasi_demeaned_regression_failed_when_sigma2_eps_is_zero`で確認・固定済み。追加のガードコードは不要（`OlsEstimator::fit`の既存の特異性検出がそのまま機能するため）。
  - **`df_resid`/`df_model`（Issue #196、7.5節）も実装済み**: `df_resid = n - k`・`df_model = k`（`k`は変換済み定数列を含む設計行列の全列数、`estimator().input().k()`）。**FEと違い自由度の再計算・独自の`cov_params`の作り直しは不要**——`OlsInput::k()`は`include_intercept`フラグの値に関わらず設計行列の実際の列数を返すため、`ReEstimator::fit`が`include_intercept=false`で変換済み定数列を`x_all`に手動追加していても、`estimator().input().k()`は既にlinearmodelsの`wx.shape[1]`（`df_resid = wy.shape[0] - wx.shape[1]`のソースで確認済み）と一致する。
    - **この副産物として、`estimator().std_errors()`/`t_stats()`/`p_values()`/`conf_lower()`/`conf_upper()`/`aic()`/`bic()`は既にこの時点で正しいRE推定量になっている**ことが判明した（`linearmodels.HomoskedasticCovariance`の`cov_type="unadjusted"`実装を確認: `debiased=True`時`nobs_eff = nobs - nvar`となり`OlsEstimator`内部の`df_resid = n - k`と同じ値になるため、`has_intercept`フラグの真偽に関わらず`X'X`とこの`df_resid`のみに依存するこれらの統計量は正しい。手動データで`linearmodels`との数値完全一致を`re_estimator_fit_matches_linearmodels_reference`に追加して固定済み）。FEのように`cov_params`を独自に作り直す必要は無い。
    - **一方`estimator().f_statistic()`/`f_p_value()`・`r_squared()`/`r_squared_adj()`はこの時点でも正しくない**——`OlsInput::from_columns`に`include_intercept=false`で渡しているため`has_intercept()==false`扱いになり、`f_statistic`は変換済み定数項も含めて同時検定してしまう。正しいF統計量はIssue #337（次項）、正しい適合度（`r_squared_within`/`between`/`overall`）はIssue #338で別途実装する（後者は`has_constant=True`のREとFE間でTSSの中心化有無が異なる点に注意、詳細はIssue #338本文参照）。
  - **F統計量（`ReEstimator::f_statistic()`/`f_p_value()`、Issue #337、2.1節）も実装済み**: 傾き係数`df_model - 1`個（定数項を除く）が同時にゼロという帰無仮説の検定。
    - **`wald_test_last_columns`（`cov_params`の部分行列を反転するWald検定、`FeEstimator`の`wald_f_test`直接再利用と同型の発想）は使えない・不均衡パネルで`linearmodels`と数値不一致になる罠**: 当初「先頭列が変換済み定数項なので`estimator().wald_test_last_columns(df_model - 1)`を呼ぶだけで済む」という実装を試みたが、不均衡パネル（θ_iがエンティティごとに異なる）データで`linearmodels.RandomEffects.fit().f_statistic`と数値が一致しなかった。原因は`linearmodels`の`_PanelModelBase._f_statistic`ソース確認で判明: 「定数項を除く」際の比較対象（`weps_const`）を、実際にモデルに含まれる変換済み定数列（`1-θ_i`、エンティティごとに異なる）ではなく**変換済みyの単純平均**（`y - mean(y)`、定数列が文字通り1のときの構成）で計算している。Wald検定（部分行列反転、実際にモデルに含まれる列を基準にする方式）とこの定義は、定数列が全観測で同一の値（バランスパネルでθが全エンティティ共通）でない限り一致しない（手動データでの数値不一致で実地確認済み）。`linearmodels`が主リファレンスのため、`ReEstimator::fit`はこの定義（変換済みyの単純平均を基準にした古典的SST/SSR比較、`wald_f_test`の再利用はしない）を直接実装している。**FE側で確立した「`wald_f_test`・`wald_test_last_columns`を再利用してサンドイッチ計算を複製しない」という一般方針は、切片が文字通り1の列でない（θ変換済みの）REのケースには無条件には適用できない**——今後同種の「切片を除く検定」を書く際は、比較対象が「モデルに実際に含まれる列」なのか「単純な標本平均」なのかをまずリファレンス実装のソースで確認すること。
    - `residual_ss<=0.0`（完全な当てはめ）なら`linearmodels`と同じくF統計量を`0.0`とする（`OlsEstimator`自身の`df_model==0`分岐のNaNとは別の扱い）。傾き係数が0個（`df_model==1`）ならOLS/FE同様NaN。**`residual_ss`は`linearmodels`ソースの`denom`（`weps.T@weps`、非制限モデル自身の残差平方和）と同じ量であり、`total_ss`側の量ではないことに注意**（rust-reviewerが変数の取り違えを疑って指摘、`re.rs`のコメントで明示済み）。この分岐自体は意図的に未テスト——`σ_ε²=0`は先に`SingularMatrix`で失敗する経路（既存テスト参照）と重なり、`σ_ε²>0`のまま`residual_ss`だけ厳密に0になる非退化データの確実な構成方法が見つからなかった（`FeEstimator::fit`の`FTestFailed`未テスト方針と同型の判断）。
    - **F統計量は負値になりうる**（`linearmodels`自身でも極端な不均衡パネルで実地確認済み。`re_estimator_fit_f_statistic_can_be_negative_for_extremely_unbalanced_panel`、T_i={2,2,15}で固定）。原因: `total_ss`（変換済みyの単純平均基準）は実際にモデルに含まれる変換済み定数列に対する直交性を持たないため、教科書的な入れ子モデル比較（`total_ss >= residual_ss`保証）とは異なり`total_ss < residual_ss`になりうる。`hausman_statistic`（負値をそのまま返す）と同じ「主リファレンスの挙動をクリップせずそのまま踏襲する」方針。
    - 失敗経路は`CommonError::ComputationFailed`（`FisherSnedecor::new`が理論上失敗しえない`num_df`/`df_resid`——ともに`OlsEstimator::fit`成功時点で`>=1`保証済み——に対する防御的`Result`化、`hausman_statistic`の`ChiSquared::new`と同じパターン）のみで、新規`PanelError`バリアントは追加していない。
    - 検証は`linearmodels`（`cov_type="unadjusted"`）の`f_statistic`と直接数値比較（相対誤差`1e-9`、`.claude/rules/testing-policy.md`の基本方針どおり）。
    - **今後の再検討候補（Issue #339）**: `plm:::pwaldtest`（Rクロスチェック）はWald二次形式（`β'V⁻¹β`）を使い理論上負値になり得ない一方、`linearmodels`のSST/SSR方式は上記の通り負値になりうる。#203のベンチマーク作成時に、`f_statistic`/`f_p_value`という1統計量に限定して`plm`定義への切り替えを再検討する（主リファレンス自体を`linearmodels`から`plm`へ全面的に変更するのではない、他の統計量は`linearmodels`と数値完全一致で検証済みのため、ユーザー確認済み・2026-09-19）。
  - **パネル固有R²（`ReEstimator::r_squared_within()`/`r_squared_between()`/`r_squared_overall()`、Issue #338、2.3節）も実装済み**: FEの`fe_r_squared_between`/`fe_r_squared_overall`（Issue #183）をそのまま流用できない・共有せずRE独自の`re_r_squared_within`/`re_r_squared_between`/`re_r_squared_overall`として実装した（`panel-api-design.md`7.4節「無理な共通化はしない」）。差異は2点:
    - **TSSの中心化有無**: REは`has_constant=True`のため中心化TSS（`Σ(y-ȳ)²`）を使うが、FEは`has_constant=False`扱いのため非中心化TSS（`Σy²`）を使う（`weights`引数はFE/REどちらも未サポートのため常に`w=1`——`_prepare_between`の`T_i`ベース重み付けはFE同様常に無効、`fe_r_squared_between`関数doc参照）。
    - **当てはめ値への切片`β0`の有無**: FEは固定効果自体を含めない「弱いR²」を意図的に採用する（`slope_only_residual`）が、REは真の切片係数`β0`（`params().get(0,0)`）を含めて当てはめる（`fitted = β0 + Σ_j x_j・β_j`）。単なる`has_intercept`分岐の追加では済まず、フィット済みの値そのものの計算式が変わるため共有関数化を見送った。
    - `r_squared_within`はFEと同じ定義（θ=1固定の通常のwithin変換、REの`θ_i`とは無関係）。共有ヘルパー`all_ones_theta`は元々`fe.rs`専用のprivate関数だったが、本Issueで`common.rs`に`pub(crate)`として移設した（`group_indices_by_key`/`count_unique`がIssue #193で同じ理由で移設された前例に倣う）。
    - `linearmodels`の`_rsquared`の早期リターン（`has_constant and exog.nvar==1`なら3種とも`0.0`）に倣い、`df_model==1`（傾き係数0個）なら3種とも`0.0`とする（`f_statistic`のNaN分岐とは異なる扱い）。
    - `r_squared_between`が負値になりうる（`f_statistic`と同型の性質、教科書的な保証が無いR²の定義のため）ことも実地確認済み（`re_estimator_fit_matches_linearmodels_reference`でRE Between R²=-0.39...を固定）。
    - 検証は`linearmodels`（`cov_type="unadjusted"`）の`rsquared_within`/`rsquared_between`/`rsquared_overall`と直接数値比較（相対誤差`1e-9`）。
  - **`cov_type`対応（`ReCovType`、Issue #197、3.1節・3.2節）も実装済み**: `linearmodels.RandomEffects.fit()`のソース確認で判明した重要な事実——**REは`cov_type`によらず常に`extra_df=0`**（FEのような`neffects`・`entity_nested_within_cluster`の条件分岐が一切不要）。REの変換済み設計行列（`x_all`）には「省略された固定効果ダミー」が無く、切片も含め全パラメータが実際に列として含まれているため、HC2/HC3のレバレッジもFEの`leverage_full`（LSDV相当の欠落ダミー補正）ではなく`leverage_within`（素の`h_ii = x_i(X'X)⁻¹x_i'`、REの実際の設計行列に対して直接計算するだけで良い）で足りる。
    - **`panel::fe`のcov_type計算関数を`common.rs`へ移設・FE/RE共有化した**（`design_matrix_from_columns`・`xtx_inverse`・`leverage_within`・`fe_classical_cov_params`→`panel_classical_cov_params`・`FeHcVariant`→`PanelHcVariant`・`fe_hc_cov_params`→`panel_hc_cov_params`・`fe_cluster_cov_params`→`panel_cluster_cov_params`・`resolve_dk_bandwidth`・`fe_driscoll_kraay_cov_params`→`panel_driscoll_kraay_cov_params`）。**数式自体はFE実装時（Issue #181・#182）から一切変更していない**——これらの関数はもともと`df_resid`・`extra_df`・レバレッジ配列を引数で受け取る汎用実装だったため、呼び出し側（`fe.rs`は`extra_df=neffects`系・`leverage_full`、`re.rs`は`extra_df=0`固定・`leverage_within`）を差し替えるだけでそのまま再利用できた。`entity_nested_within_cluster`・`leverage_full`はFE専用のまま`fe.rs`に残置（RE不要のため）。`wald_f_test`をFEが`pub(crate)`化して再利用した前例（4.2節）と同じ「サンドイッチ計算を複製しない」判断——移設前に手動データで`linearmodels`（Classical/HC1/Cluster/HAC）・`plm`（HC2/HC3）との数値一致を実地検証してから着手した（詳細は`re.rs`の`ReEstimator::fit`docコメント「`cov_type`対応」参照）。fe.rsの既存594テスト全て無変更で通ることをリファクタ直後に確認済み（数式の等価性の裏付け）。
    - **`ReEstimator`は常に自前のフィールド（`std_errors`/`t_stats`/`p_values`/`conf_lower`/`conf_upper`/`cov_type`）を保持する**（`estimator()`委譲には頼らない、ユーザー確認済み・2026-09-19）。Classical/HC1のような「`OlsEstimator`委譲でも数値的に正しい」cov_typeであっても、Cluster/HACとの非対称なAPIを避けるため、全cov_typeで一貫して独自計算・独自フィールド保持にした（`FeEstimator`と同型のパターンに揃える）。
    - **HC2/HC3の参照実装が無い**: `linearmodels`は`RandomEffects`・`PanelOLS`（FE）どちらも`_cov_estimators`に単一の"heteroskedastic"（HC1相当）しか持たず、HC2/HC3を提供していない（FEのHC2/HC3実装が実際には`fixest`を参照値にしていたのと同じ理由）。REは`plm::vcovHC(fit, method="white1", type="HC2"/"HC3")`をクロスチェックに使う（ユーザー確認済み）。`plm`は変量効果の分散成分推定法が`linearmodels`と僅かに異なる（点推定自体が僅かに違う）ため、Classical/HC1/Cluster/HACほどの精度（1e-9）ではなくクロスチェック水準（`re_estimator_fit_hc2_hc3_match_plm_reference`で1e-2）で検証する。
    - `ReCovType::Cluster`の`q`（傾き係数の数、切片を除く）は`df_model - 1`（OLS自身の`k - k_constant`と同じ規約）。`ReCovType::Hac`は`FeCovType::Hac`と異なり`time`オーバーライドフィールドを持たない（RE自身が2-way構造を持たずFEのような「2-way構造とDK時間粒度の分離」ニーズが無いため、`input.time()`を直接使う設計に単純化した）。
    - 検証は`linearmodels`（Classical/HC1/Cluster/HAC、いずれも1e-9で数値完全一致）・`plm`（HC2/HC3、上記の通りクロスチェック水準）。
  - `fit()`本体（ハウスマン検定、Issue #198）はまだ未実装。`hausman_statistic` も現時点ではまだ`#[cfg(test)] mod tests`からのみ呼ばれる（`pub fn`なので`dead_code`にはならない）。
- **カバレッジ監査（Issue #185）実施済み**: `cargo llvm-cov -p engine`でFE関連（`panel/fe.rs`・`panel/common.rs`）を計測し、OLS/Logit/Probitと同水準（region/function/line いずれも97〜99%台）であることを確認した。境界値・悪条件（singleton・不均衡2-way・分散ゼロ変数・悪条件設計行列＝完全多重共線性）は既存テストで網羅済み。残る未カバー箇所は全て「保証済みの不変条件に対する防御的`Result`化」（`xtx_inverse`のCholesky失敗、`OlsInput::from_columns`のDimensionMismatch、`StudentsT::new`の自由度不正——いずれも`fit()`内の事前バリデーションで到達不能なことをdocコメントで明記済み、`.claude/rules/rust-style.md`「テスト」節の許容パターン）と、テストのアサーション失敗メッセージ内の行（テスト成功時は実行されない）のみ。追加で`FeEstimator::input()`/`cov_type()`の単純getterが未検証だったため`fe_estimator_fit_exposes_input_and_cov_type_via_getters`（`ols::fit_exposes_input_cov_type_and_residuals_via_getters`と同型）を追加した。

## faerのグローバル並列度（Issue #283）

- FE/RE の `fit()` エントリを実装するときは、**冒頭で `crate::parallelism::ensure_serial()` を呼ぶこと**（faer のグローバル並列度を `Par::Seq` に固定。OLS/WLS/Logit/Probit/Tobit/2SLS/GMM の各 `fit()` と同じ）。理由・背景は `engine/src/linear/CLAUDE.md`「faerのグローバル並列度」と `.claude/rules/rust-style.md`「パフォーマンス」節を参照。FE は within 変換後に `OlsEstimator::fit` へ委譲するため OLS 側の呼び出しでも一応担保されるが、`cargo test -p engine` で `PanelEstimator::fit` を直接叩く経路との統一のため各 `fit()` からも呼ぶ。回帰ガード（`fit_pins_faer_global_parallelism_to_seq`）も他系統に倣って1本入れる。

## 設計上の決定（再発見コスト削減）

- **`engine` はpolars非依存。`panel-api-design.md` 6.1節の「polarsの`group_by`で実装」は`engine_pybind`層／抽出後配列の話**。`engine`側のグループ集約は、クラスター列と同じく `entity: &[String]`（長さ`n`、行はパネル観測順）を受け取る。
- **`quasi_demean_column` の内部集約は `HashMap` でよい（`cluster_cov_params` の `BTreeMap` 必須とは別）**。あるエンティティの和は観測順（入力行の固定順）に積まれ、各行の出力はそのエンティティの和だけに依存する。エンティティ「間」をまたぐ加算（`Σ_g S_g S_g'` のようにグループ順序が浮動小数点丸めに効く演算）が無いため反復順序非依存でビット単位決定的。`HashMap` で集約・引き当てが O(n log G) → O(n)。**クラスターロバスト分散等、グループ間加算があるパネル系の実装を今後書くときは `engine/src/linear/CLAUDE.md` の `BTreeMap` 方針に戻ること**。
- **`quasi_demean_column` は列単位の関数**（`y`/`x`をまとめて受けない）。呼び出し側（FE/REの`fit()`）が `y` と `x` の各列にループ適用し、変換後の列を `OlsEstimator::fit` に渡す（WLSがsqrt(w)変換データをOLSへ委譲するのと同型、4.3節・7.4節）。列ごとに独立な変換のため単体テストが単純になる。
- **θの渡し方は `&BTreeMap<String, f64>`（エンティティID→θ_i）**。観測単位の`&[f64]`や「エンティティ順の`&[f64]`」にしない（順序規約をFE/REと共有する必要が生じ、ミスの余地が残るため）。**FEはこの関数の `θ_i = 1.0`（全エンティティ）の特殊ケース**として扱う（`panel-api-design.md` 7.4節）。REは `θ_i = 1 - sqrt(σ_ε² / (T_i·σ_u² + σ_ε²))`（7.2節）。
- **`quasi_demean_column` はグループ平均（`ȳ_i.`）を返さない**（Issue #173スコープ外）。**`fixed_effects()`（6.6節、Issue #184）はこの関数を拡張せず実装済み**——`FeEstimator::fixed_effects()`は`fit()`が既に保持している`input`（変換前の元の`y`/`x`/`entity`/`time`）と`estimator().params()`から`slope_only_residual`/`group_residual_means`で都度再計算する（`quasi_demean_column`はwithin変換専用のまま据え置き）。σ_ε²再利用（7.4節、RE実装）で平均の保持が必要になったら、その時点で改めて検討する。
- **契約違反（`entity`と列の長さ不一致・`theta`のキー欠け）は`assert!`/`expect`でpanic**（`Result`を返さない）。ユーザー入力起因ではなく`engine_pybind`〜`engine`間の内部契約違反のため、`validate_cluster_groups`の`assert_eq!`と同じ扱い。`hausman_statistic`のshape契約（`beta_fe`/`beta_re`同長・`cov`が`k×k`）も同様に`assert!`。

- **`hausman_statistic` の設計（Issue #174、7.3節）**:
  - 入力は `beta_*: &[f64]` / `cov_*: &[Vec<f64>]`（`newton_step` と同じ形。RE呼び出し側が `Mat` から一度変換）。戻り値 `Result<(stat, df, p_value), CommonError>`。
  - **比較対象のalign（重なるスロープ係数のみ、REの切片・時間不変変数を除外）は呼び出し側（RE実装）の責務**。この関数は渡された `k` 個をそのまま使う。
  - 差行列 `Var(β_FE) - Var(β_RE)` は対称だが有限標本で非正定値になりうるため、Choleskyではなく `col_piv_qr` + `solve_lstsq`（`newton_step` と同じ相対閾値・NaN明示チェックの特異性検出）。
  - **統計量が負でもそのまま返す**（`plm::phtest` と同じ。`stat<=0` なら `p_value == 1.0`）。差行列が数値的に特異なときだけ `CommonError::ComputationFailed`。
  - **p値は `chi2.sf(stat)`（`1.0 - chi2.cdf(stat)` ではない）**。大きい `stat` で `cdf ≈ 1` になり小さいp値の相対精度が失われるのを避けるため（`sf` は正則化上側不完全ガンマを直接計算。`stat<=0` でも `1.0` を返すので負統計量の挙動は不変）。**`iv/gmm.rs` の Hansen J・Wald系は今も `1.0 - chi2.cdf` のまま**——一括で `sf` へ移行するかは別issue（このズレは意図的な暫定）。
  - `df` には常に `k` を使う。差行列の実効ランクが `k` 未満のとき `stat` と `df` に不整合が生じうる（`plm::phtest` も同じ制約）。
  - v1は classical Hausman のみ（`cov_type` 非依存）。robust版は将来issue。

## 踏んだ罠: between回帰でエンティティ平均が完全に一致すると`OlsEstimator::fit`が`StudentsT::cdf(NaN)`でパニックする（Issue #338、本Issueのスコープ外の既存バグ）

- Issue #338（パネル固有R²）の`re_r_squared_between`のテスト作成中に発見した。**`swamy_arora_variance_components`のbetween回帰（`OlsEstimator::fit`）と`re_r_squared_between`は無関係だが、同じデータ条件（全エンティティの`ȳ_i.`が完全に一致）で同時に踏む**——REのbetween R²のTSSが0になる条件（`Σ(ȳ_i.-grand_mean)²=0`）は、between回帰自身が「切片=共通の平均値・傾き=0」という完全な当てはめ（`SSR_between=0`）になる条件と全く同じであるため。
- `SSR_between=0`だと`classical_cov_params`の`σ²=0`となり、between回帰の**全係数**の`std_error=0`になる。もし共通の平均値自体も0（＝切片の係数も0）なら、`OlsEstimator::fit`内の`compute_inference_stat`（`crate::inference.rs`）が`stat = coef/se = 0.0/0.0 = NaN`を計算し、続く`dist.cdf(stat.abs())`（`StudentsT::cdf`）が`statrs`の`beta_reg`内部で`XOutOfRange`としてパニックする。
- **`OlsEstimator::fit`・`crate::inference::compute_inference_stat`はこのNaN t統計量をガードしていない**（本来`ValidationError`/`ComputationError`として`Result`で返すべきだが、現状は`panic`する）。OLS/WLS/FE/RE/Logit/Probit/Tobit/2SLS/GMMなど`compute_inference_stat`を経由する全手法に共通するリスクだが、他手法の既存テストでは偶然一度も踏んでいなかった。修正は本Issueのスコープ外のため対応していない——**Issue #340として切り出し済み**（`k=0`の構造的退化を扱うIssue #140とはトリガー条件・原因のクレード（`statrs`対`faer`）が異なる別問題、#340本文の比較表参照）。
- **回避策（`re_r_squared_between`のテストで採用）**: エンティティ平均を「全て同じ非ゼロ値」にすると、切片の係数自体は非ゼロになり`t統計量=非ゼロ/0=±∞`（`NaN`ではない）になるためこのパニックを回避できる（`statrs`は無限大の`t統計量`自体は正しく処理できる。`re_estimator_fit_r_squared_between_returns_zero_when_entity_means_are_equal`参照）。REのbetween R²のTSSはyの水準シフトに対して不変（中心化TSSのため）なので、この回避策でもテストしたい退化条件（TSS=0）自体は変わらない。
- 今後、between回帰・その他の回帰で「エンティティ（またはグループ）平均が完全に一致する」ような退化データをテストで構成する際は、この罠に注意すること（同じ理由で対称的に踏む）。

## PanelError（Issue #172で確定済みの方針、変更時は要確認）

- FE/REで共有（`FeError`/`ReError`は作らない）。`CommonError` を `#[error(transparent)] Common(#[from] CommonError)` で包む（`LeastSquaresError`/`MleError`/`IvError` と同じ）。
- `WithinRegressionFailed { #[source] source: LeastSquaresError }` は `#[from]` を使わず明示的に `.map_err` で包む（`CommonError` が2経路で `PanelError` になる曖昧さを避けるため。`IvError::FirstStageFailed` と同じ判断）。`FTestFailed { #[source] source: LeastSquaresError }`（Issue #186）も同じ`LeastSquaresError`ラップだが、`OlsEstimator::fit`委譲の失敗ではなくFE独自のF検定（`wald_f_test`再利用）の失敗を表すため別バリアントにしている。
- RE固有バリアント（between回帰の自由度不足、Hausman統計量の非正定値ケース等）は未定義。RE実装issueで計算コードを書く過程で追加する（`common.rs` モジュールdocコメントの「追加候補」参照）。
